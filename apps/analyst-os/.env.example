# Studio is a separate app (see apps/studio). In production it's usually
# reached at a same-origin path behind a reverse proxy, but locally it runs
# on its own Vite dev server, so point this at it directly.
VITE_STUDIO_URL=http://localhost:8080
